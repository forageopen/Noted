{
  "name": "noted",
  "version": "0.1.0",
  "private": true,
  "description": "Noted (FOGA) - browser-native Markdown viewer/editor",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/main.ts --bundle --format=esm --target=es2022 --outfile=dist/main.js --external:buffer"
  },
  "dependencies": {
    "marked": "^13.0.0",
    "docx": "^8.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.1.0",
    "esbuild": "^0.23.0"
  },
  "engines": {
    "node": ">=20"
  }
}
